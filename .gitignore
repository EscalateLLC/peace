# dependencies
node_modules/

# builds
dist/
build/
out/
.next/
*.tsbuildinfo

# local data (SQLite lives outside git)
*.db
*.db-wal
*.db-shm

# generated audio assets (regenerate via pnpm --filter @peace/bot-discord gen-offline-clip)
*.pcm

# env / secrets
.env
.env.*
!.env.example

# logs / coverage
.logs/
coverage/
*.log

# os / editor
.DS_Store
Thumbs.db

# Playwright MCP scratch output (snapshots, screenshots)
.playwright-mcp/

.claude
# root-level agent scratch dir only — must NOT match packages/agent
/agent/
/internal
