// Moved to @peace/core (so the router can reuse it without depending on all of
// @peace/session). Re-exported here for backward compatibility.
export { matchWakePhrase, type WakeMatch } from '@peace/core';
